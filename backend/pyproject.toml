[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
	"alembic",
	"asyncpg",
	"fastapi",
	"sqlalchemy",
	"uvicorn",
	"pydantic",
	"pydantic[email]",
	"psycopg2",
	"bcrypt",
	"PyJWT",
	"langgraph>=0.2.0",
    "langchain>=0.3.0",
    "langchain-aws>=0.2.0",
    "langchain-google",
    "langchain-openai",
    "langchain-core>=0.3.0",
    "boto3>=1.35.0"
]

[dependency-groups]
dev = [
	"ruff",
	"python-dotenv",
    "debugpy"
]

test = [
    "pytest",
    "pytest-asyncio",
    "httpx",
    "aiosqlite",
]
 

